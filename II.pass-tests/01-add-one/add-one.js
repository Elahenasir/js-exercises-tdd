// var numbers = [8, 12, 5, 2, 5, 7];
 function addOneToArray(arr) {
            for (var i = 0; i < arr.length; i ++){
            arr[i]+=1
        }
        }
        module.exports=addOneToArray;
        // addOneToArray(numbers);
        //     console.log(numbers)
     